# Briefing

Repositório inicial com estrutura de front-end estática.

- index.html (homepage)
- css/ (base, layout, components)
- images/ (logo, banners, products)
- icons/, fonts/, favicons/

Notas:
- Substituir fontes self-hosted em `fonts/` quando disponíveis.
- Colocar SVGs em `icons/` e favicons em `favicons/`.
